Database anvica_radius

ALTER TABLE identities
ADD COLUMN country VARCHAR(100) NULL AFTER name,
ADD COLUMN state VARCHAR(100) NULL AFTER country,
ADD COLUMN city VARCHAR(100) NULL AFTER state,
ADD COLUMN zone VARCHAR(100) NULL AFTER city,
ADD COLUMN area VARCHAR(100) NULL AFTER zone,
ADD COLUMN society ENUM('super premium', 'premium') DEFAULT 'premium' AFTER area,
ADD COLUMN otp_sms TINYINT(1) DEFAULT 0 AFTER society,
ADD COLUMN otp_whatsapp TINYINT(1) DEFAULT 0 AFTER otp_sms,
ADD COLUMN otp_email TINYINT(1) DEFAULT 0 AFTER otp_whatsapp;